name: irhebo
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.1.0+30

environment:
  sdk: ^3.6.1


dependencies:
  flutter:
    sdk: flutter
  get:
  bot_toast:
  path: 
  # path_provider:
  # flutter_statusbarcolor_ns:
  cupertino_back_gesture:
  http:
  oktoast:
  logger:
  # retry:
  url_launcher:
  get_it:
  shared_preferences:
  flutter_countdown_timer:
  dropdown_search: 
  flutter_svg:
  multi_select_flutter:
    path: 'packages/multi_select_flutter_4.1.3/'
  permission_handler: 
  audio_waveforms:
  path_provider:
  readmore:
  file_picker: ^10.1.9
  mime: #1.0.4 #^1.0.5 
  flutter_animate:
  flutter_slidable:
  flutter_screenutil:
  # pusher_channels_flutter:
  #video_player:
  # video_thumbnail:
  #open_filex:
  #voice_message_package: any
  image_picker: ^1.1.2
  #
  cached_network_image:
  internet_connection_checker:
  dio:
  dartz:
  #just_audio: ^0.9.37
  showcaseview:
  flutter_widget_from_html:
  flutter_localizations:
    sdk: flutter
  fluttertoast:
  flutter_rating_bar:
  flutter_staggered_grid_view:
  shimmer:
  json_annotation:
  rounded_loading_button:
  cupertino_icons: 
  pinput: 
  connectivity_plus:
  another_flushbar:
  country_code_picker:  
  pull_to_refresh: 
  intl:
  gradient_borders: 
  provider: ^6.1.5
  talker_dio_logger: ^4.9.0
  onesignal_flutter: ^5.3.3
  firebase_core: ^3.14.0
  timeago: ^3.7.1
  pusher_channels_flutter: ^2.5.0
  video_player: ^2.10.0
  google_sign_in: ^6.3.0
  audioplayers: ^6.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 
  json_serializable: 
  build_runner: 
  # flutter_launcher_icons:

# flutter_launcher_icons:
#   android: "launcher_icon"
#   ios: true
#   image_path: "assets/images/usercards_logo.png"
#   min_sdk_android: 21 # android min sdk min:16, default 21

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/fonts/
    - assets/sounds/

  #   - images/a_dot_ham.jpeg
  fonts:
    - family: Mukta
      fonts:
        - asset: assets/fonts/mukta/mukta-regular.ttf
        - asset: assets/fonts/mukta/mukta-medium.ttf
        - asset: assets/fonts/mukta/mukta-bold.ttf